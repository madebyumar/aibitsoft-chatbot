services:
  - type: web
    name: aibitsoft-chatbot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn src.server:app --host 0.0.0.0 --port 10000"
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
